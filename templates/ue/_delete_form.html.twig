<form method="post" action="{{ path('app_u_e_delete', {'idUE': u_e.idUE}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ u_e.idUE) }}">
    <button class="btn">Delete</button>
</form>
