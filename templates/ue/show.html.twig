{% extends 'base.html.twig' %} {% block title %}UE{% endblock %} {% block body
%}
<h1>UE</h1>

<table class="table">
  <tbody>
    <tr>
      <th>IdUE</th>
      <td>{{ ue.idUE }}</td>
    </tr>
    <tr>
      <th>CodeUE</th>
      <td>{{ ue.codeUE }}</td>
    </tr>
    <tr>
      <th>NomUE</th>
      <td>{{ ue.nomUE }}</td>
    </tr>
    <tr>
      <th>ImageUE</th>
      <td>{{ ue.imageUE }}</td>
    </tr>
  </tbody>
</table>

<a href="{{ path('app_u_e_index') }}">back to list</a>

<a href="{{ path('app_u_e_edit', { idUE: ue.idUE }) }}">edit</a>

{{ include("ue/_delete_form.html.twig") }}
{% endblock %}
